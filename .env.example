# Secret key for Flask sessions
SECRET_KEY=dev_secret_key_resume_booster_2026

# MySQL Database Configurations (Leave DB_USER empty to fallback to SQLite)
DB_USER=root
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=3306
DB_NAME=resume_booster
